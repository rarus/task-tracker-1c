
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ТекстОшибки = "";
	ИмяСправочникаПользователей = тз_ОбщегоНазначенияВызовСервераТрекер.ПолучитьОпределяемыТипСтрокой("тз_Этапы", ТекстОшибки);
	Если ИмяСправочникаПользователей = Неопределено Тогда
		ВызватьИсключение ТекстОшибки;
	КонецЕсли;
	ОткрытьФорму("Справочник." + ИмяСправочникаПользователей + ".ФормаСписка");
КонецПроцедуры